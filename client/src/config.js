// Configuración global para la aplicación React
const config = {
  // En producción podrías usar import.meta.env.VITE_API_URL
  apiUrl: 'http://localhost:3000/api',
  socketUrl: 'http://localhost:3000'
};

export default config;
